# Optimized dependencies for Vercel deployment
# Core FastAPI dependencies
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Essential utilities
PyYAML==6.0.1
aiofiles==23.2.1
requests==2.31.0
httpx==0.25.2

# AI/ML clients (lightweight)
openai==1.3.7

# GitHub integration
PyGithub==1.59.1
gitpython==3.1.40

# Code analysis (lightweight)
bandit==1.7.5

# Utilities
rich==13.7.0
jinja2==3.1.2

# Removed heavy dependencies for serverless:
# - streamlit (not needed for API)
# - plotly, pandas, numpy, scikit-learn (not needed for core API)
# - redis, celery (serverless doesn't support long-running workers)
# - docker, psutil (not available in serverless)
# - sqlalchemy, alembic (use external DB if needed)
# - development tools (pytest, black, mypy, etc.)
